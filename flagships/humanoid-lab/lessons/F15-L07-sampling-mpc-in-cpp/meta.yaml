id: F15-L07-sampling-mpc-in-cpp
title: Implement the MPPI sampling loop in C++ and measure the rollout throughput a 100 Hz control tick can afford
track: T11
flagship: F15-humanoid-lab
tier: cpu8              # phone | browser | cpu8 | free-gpu | gpu24 | gpu80 | multi-gpu | api
budget_seconds: 120
language: cpp           # python | c | cpp
prerequisites:
  - F15-L01-first-contact       # mjModel vs mjData, stepping, reading state
  - F15-L03-pd-control          # the feedback law this lesson shows the limits of
  - F15-L06-control-loop-in-c   # building against libmujoco from a compiled language
objectives:
  - "Implement the three stages of an MPPI planner in C++ — sample, score and refit — and pass the binary's self-test"
  - "Measure this machine's rollout throughput in C++ and in Python on identical work, and report the ratio the two loops achieve"
  - "Compute the sample-and-horizon budget a 100 Hz control tick affords at a measured throughput, and decide whether a configuration is real-time"
  - "Explain why subtracting the minimum cost before exponentiating is what stops the MPPI weights underflowing to zero"
  - "Show that scoring a candidate plan leaves the live simulation state untouched, and explain what breaks when it does not"
datasets:
  - name: "cartpole.xml — cart-pole MJCF authored for this lesson"
    licence: "Authored for The AI Atlas; no third-party rights attach to it. Written against the MJCF schema."
    url: ""               # none: the file is authored here, ships in assets/, and is never fetched
    licence_url: "https://mujoco.readthedocs.io/en/stable/XMLreference.html"
    engine_licence: "MuJoCo itself is Apache-2.0 — https://raw.githubusercontent.com/google-deepmind/mujoco/main/LICENSE"
    cached_at: "assets/cartpole.xml"
    bytes: 2682
    retrieved: 2026-09-16
    gated: false
    notes: >-
      There is no dataset to download and therefore nothing gated, licensed or registration-
      walled on any required path. The lesson's only asset is four rigid-body parameters and
      one actuator, written out in full: no mesh, no include, no file= reference. It runs with
      the network switched off. Provenance and the reasoning behind each modelling choice are
      recorded in assets/SOURCE.md; every world-fact that file leans on — the documented
      default timestep, the integrator list, the absence of a cart-pole in MuJoCo's own
      model/ tree, and dm_control's cart-pole needing its common/ includes — is sourced in
      claims.yaml.
points: 53
status: reviewed        # draft | executed | reviewed | shipped
reviewed_by: "F15-L07 review pass, 2026-09-16 — all 12 QUALITY.md gates re-checked against a clean rebuild. Fixed: a 75-line prose cell (gate 3); a rubric that could not distinguish flooring from rounding in max_samples, nor <= from < in realtime (gate 6); two unsourced world-claims in assets/SOURCE.md and one non-verbatim quote in claims.yaml (gate 12). Open: two of the three declared prerequisites are still unauthored template stubs (gate 2)."
measured_seconds: 3.6
measured_peak_mib: 50
