# Build products of this lesson: the compiled binaries (build/lesson from the student's
# lesson.cpp, build/lesson_ref from solutions/lesson.cpp) and the corpus slices the notebook
# and the rubric write for the two trainers to read. Every file here is regenerated by `make`
# or by the lesson itself; none of it is authored, and none of it belongs in a checkout.
#
# assets/ is NOT ignored: corpus.txt ships with the lesson so it runs with no network.
build/
__pycache__/
